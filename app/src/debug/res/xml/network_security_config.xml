<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG OVERRIDE of app/src/main/res/xml/network_security_config.xml.

     Deliberately omits the <pin-set> so developers can inspect traffic through
     Charles / mitmproxy / Proxyman on a debug build. Release builds use the main
     config, which pins the Google Trust Services chain.

     Keep this file free of pins — adding them here defeats its purpose. -->
<network-security-config>

    <!-- Local dev servers over cleartext (AVD 10.0.2.2, LAN device) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>

</network-security-config>
